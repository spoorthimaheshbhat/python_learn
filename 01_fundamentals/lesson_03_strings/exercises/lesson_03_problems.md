# Lesson 3 — Strings — Exercise Problems

# Exercise 1 — String Indexing

Create:

```python id="6i3r2p"
language = "Python"
```

Print:

* first character
* last character
* third character

using indexing.

---

# Exercise 2 — String Slicing

Using:

```python id="0v8r1m"
word = "Programming"
```

Print:

* first 4 characters
* last 4 characters
* reversed string

using slicing.

---

# Exercise 3 — String Methods

Create:

```python id="0d1f4y"
text = "   python programming   "
```

Perform:

* remove spaces
* convert to uppercase
* replace `"python"` with `"Java"`
* count occurrences of `"m"`

---

# Exercise 4 — String Concatenation

Take:

* first name
* last name

from user input.

Print full name using concatenation.

---

# Exercise 5 — f-Strings

Take:

* name
* age
* favorite language

from user input.

Print:

```text id="7q9m3l"
My name is ___, I am ___ years old, and my favorite language is ___.
```

using f-strings.

---

# Exercise 6 — split()

Create:

```python id="6m5u2r"
fruits = "apple,mango,banana,grapes"
```

Convert it into a list using `split()`.

Print:

* full list
* first fruit
* last fruit

---

# Exercise 7 — String Immutability

Predict output BEFORE running:

```python id="3t9u1s"
text = "Python"
text[0] = "J"

print(text)
```

Explain WHY the output/error occurs.
