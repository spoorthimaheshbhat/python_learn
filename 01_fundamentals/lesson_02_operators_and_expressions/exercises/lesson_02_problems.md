# Lesson 2 — Operators and Expressions — Exercise Problems

# Exercise 1 — Arithmetic Practice

Create two variables:

```python
a = 15
b = 4
```

Print:

* addition
* subtraction
* multiplication
* division
* floor division
* modulus
* exponent

---

# Exercise 2 — Even or Odd

Write a program that checks whether a number is:

* even
* odd

using `%`.

---

# Exercise 3 — Comparison Operators

Given:

```python
x = 10
y = 20
```

Print results for:

* `x == y`
* `x != y`
* `x > y`
* `x < y`
* `x >= y`
* `x <= y`

---

# Exercise 4 — Logical Operators

Create:

* `age`
* `has_ticket`

Check:

* if a person can enter a movie theater

Condition:

* `age >= 18`
* AND has ticket

---

# Exercise 5 — Membership Operators

Check:

* whether `"python"` contains `"th"`
* whether `"java"` contains `"p"`

using `in`.

---

# Exercise 6 — Identity Operators

Predict the output BEFORE running:

```python
a = [1, 2, 3]
b = a
c = [1, 2, 3]

print(a is b)
print(a is c)
print(a == c)
```

Explain why the outputs occur.
